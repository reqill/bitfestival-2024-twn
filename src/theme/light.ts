import { ThemeOptions } from '@mui/material';
import { THEME_MODE } from '@/constants/theme';

export const light: ThemeOptions = {
  // exclusive light theme options
  palette: {
    mode: THEME_MODE.LIGHT
  }
};

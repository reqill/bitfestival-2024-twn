import { ThemeOptions } from '@mui/material';
import { THEME_MODE } from '@/constants/theme';

export const dark: ThemeOptions = {
  // exclusive dark theme options
  palette: {
    mode: THEME_MODE.DARK
  }
};
